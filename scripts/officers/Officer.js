export const Officer = (officer) => {
    return `<section class="officer__card" id="${officer.id}">
                <div class="officer__name"><h4>${officer.name}</h4></div>
            </section>`
}
